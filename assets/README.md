# zimzim visual assets

The prototype keeps the illustration system inline as SVG so it remains lightweight and works in the sandboxed preview without external image or font dependencies. All marks are drawn in the HTML and styled through the shared visual system.
